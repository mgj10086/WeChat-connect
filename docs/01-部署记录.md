# 部署记录

> 在此记录部署过程的每一步。

## 环境

- **操作系统**：Windows 11
- **Node.js 版本**：
- **claude CLI 版本**：
- **wechat-claude-code 版本**：
- **模型**：deepseek-v4-flash[1m]
- **API 端点**：https://api.deepseek.com/anthropic

## 部署步骤

### 1. 环境检查

- [ ] Node.js >= 18 已安装
- [ ] claude CLI 在 PATH 中（`claude --version`）
- [ ] `ANTHROPIC_BASE_URL` 环境变量已设置
- [ ] `ANTHROPIC_AUTH_TOKEN` 环境变量已设置

### 2. 安装依赖

- [ ] `cd src && npm install` 成功
- [ ] 构建产物 `dist/main.js` 存在

### 3. 扫码登录

- [ ] `node src/dist/main.js setup` 二维码正常生成
- [ ] 微信扫码成功
- [ ] `~/.wechat-claude-code/accounts/` 下生成账号文件

### 4. 启动验证

- [ ] daemon 启动无报错
- [ ] 微信发消息，Claude 正常回复
- [ ] 回复模型正确（flash）

## 备注

-
